import Typography from '@mui/material/Typography';
import { memo } from 'react';

export const ObjectViewUnknown = memo(() => (
  <Typography component="span" color="gray.500" fontStyle="italic">
    unknown
  </Typography>
));
