import Typography from '@mui/material/Typography';
import { memo } from 'react';

export const ObjectViewUndefined = memo(() => (
  <Typography component="span" color="gray.500" fontFamily="monospace">
    undefined
  </Typography>
));
